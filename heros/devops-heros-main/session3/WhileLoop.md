# While Loop Script (`while_loop.sh`)

# Learning
This script runs an infinite loop that repeatedly prompts the user to enter a number or `'q'` to quit. It validates the user's input to ensure only valid non-negative integers are accepted. 
If we get a wrong input, it prints "Invalid input"

